div(ng-show='AttachmentsService.originalAttachments.length == 0', style='padding-bottom: 10px;') No attachments added
div.panel.panel-default
  div.panel-heading Attached Files
  table.table.table-hover.table-bordered(ng-show='AttachmentsService.originalAttachments.length > 0')
    thead
      th Filename
      th Description
      th Size
      th Attached
      th Attached By
      th Delete
    tbody
      tr(ng-repeat='attachment in AttachmentsService.originalAttachments')
        td
          a(ng-hide='attachment.uri == null', href='{{attachment.uri}}') {{attachment.file_name}}
          div(ng-show='attachment.uri == null') {{attachment.file_name}}
        td {{attachment.description}}
        td {{attachment.length | bytes}}
        td {{attachment.created_date | date:'medium'}}
        td {{attachment.created_by}}
        td
          div(ng-show='disabled') Delete
          a(ng-click='AttachmentsService.removeOriginalAttachment($index)', ng-hide='disabled') Delete
